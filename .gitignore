# Python
venv/
__pycache__/
*.pyc
*.pyo
.env

# OAuth tokens (sensitive - user specific)
token.json
token.json.backup

# Per-account OAuth tokens + account index (Phase 4a multi-account switcher) -
# local non-Docker layout puts these at the repo root, sibling to token.json,
# rather than under data/
tokens/
accounts.json

# Credentials (sensitive - keep secret!)
credentials.json

# Docker-mounted data volume (tokens, routines, operation log, auth hash)
data/
auth.json
operations.json

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Personal notes (not committed)
IMPROVEMENTS.md

# Fork planning spec (needed on disk for Claude Code, not for public history)
PRD.md

# Session continuity log for AI coding assistants (local state, not public
# history — see CLAUDE.md for why this stays out of git)
PROGRESS.md

# Test/Coverage artifacts
.coverage
.pytest_cache/
htmlcov/
coverage.xml

# GitHub Copilot instructions (user specific)
.github/copilot-instructions.md
